//Remove Duplicate characters from String

//Algorithm

//1. Break the input string into arrya of character
//2. run a for loop to check the array
//3. Compare the each character with other elements 
//4. If element found remove the element from array
//5. Join the array again into string

function duplicateCharacters(str){
    arr=str.split("");
    for(let i=0;i<=arr.length();i++)
    {
        for(let j=i;j<=arr.length();j++)c
        {

        }
    }
    
}
duplicateCharacters("rajeasha");